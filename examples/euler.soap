input (
    u: [0.0, 1.0][0, 0],
    w: [0.0, 1.0][0, 0],
    n: 20,
    dt: [0.1, 0.1]
);
output (u);

i := 0;
v := 0.0;
while (i < n) (
    u0 := u + v * dt;
    v := v - w * u * dt;
    u := u0;
    i := i + 1;
);
