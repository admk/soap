input (
    x: [0.0, 1.0][0, 0], w: [0.0, 1.0][0, 0], n: 10, dt: [0.1, 0.1]
);
output (x);

i := 0;
v := 0.0;
while (i < n) (
    x0 := x + v * dt;
    v := v - w * x * dt;
    x := x0;
    i := i + 1;
);
