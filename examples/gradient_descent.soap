input (d: [0.01, 0.01][0, 0], n: [10, 20]);
output (x);

x := 0.0;
y := 0.0;
i := 0;
while (i < n) (
    i := i + 1;
    x1 := x - d * (-2 * (1 - x) - 200 * (y - x * x) * 2 * x);
    y := y - d * 200 * (y - x * x);
    x := x1;
);
