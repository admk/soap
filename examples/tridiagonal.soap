def tridiagonal(
        real[100] x=[0.0, 1.0], real[100] y=[0.0, 1.0],
        real[100] z=[0.0, 1.0]) {
    for (int i=1 ; i<100 ; i = i + 2 ) {
        x[i] = z[i]*( y[i] - x[i - 1] );
    }
    return x;
}
