# finds the root of `x ^ 2 = 3` by bisection
input (a: [0.0,0.5], b: [1.5, 2.0]);
output (x);

delta := 0.001;
i := 0;
while (b > a + delta) (
    i := i + 1;
    x := (a + b) / 2;
    y := x * x - 3;
    if (y > 0) (
        b := x;
    ) (
        a := x;
    );
);
