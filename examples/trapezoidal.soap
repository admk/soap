input (
    a: 0.0,
    b: [10, 20][0, 0],
    n: [10, 20]
);
output (y);

d := (b - a) / n;
x := a;
y := (1.0 / 2.0) / (1 + x * x);
i := 0;
while (i < n - 1) (
    i := i + 1;
    x := x + d;
    y := y + 1.0 / (1 + x * x);
);
y := (y + (1.0 / 2.0) / (1 + x * x)) * d;
