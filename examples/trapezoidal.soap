input (
    a: 0.0,
    b: [10, 20][0, 0],
    n: [10, 20]
);
output (y);

d := (b - a) / n;
x := a;
y := 1.0 / (1 + x * x) / 2.0;
while (x < b - d) (
    x := x + d;
    y := y + 1.0 / (1 + x * x);
);
y := y + 1.0 / (1 + x * x) / 2.0;
y := y * d;
