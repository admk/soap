To host the SOAP web interface via Dropbox, simply create a symlink from the Public folder of your Dropbox to this folder.

For example:

cd ~/Dropbox/Public && rm -f soap-www && ln -s /Users/jpw48/git/soap/www/ soap-www